import '../models/level_model.dart';

// Рівні Близнюків — розробляються
// Повертаємо null → constellation_screen покаже «незабаром»
class GeminiLevels {
  static LevelData? getByIndex(int levelIndex) => null;
}
